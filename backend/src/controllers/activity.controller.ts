import { Request, Response } from "express";

export const createActivity = async (req: Request, res: Response) => {
  // type, related ids
};
export const getActivity = async (req: Request, res: Response) => {
  // Query: entity=contact, id=123
};
